class Solution {
    public void reverseString(char[] s) {
        int n=s.length;
        List <Character> m=new ArrayList<Character>();
        for(int i=n-1;i>=0;i--){
            m.add(s[i]);
        }
        for(int i=0;i<n;i++){
            s[i]=m.get(i);
            
        }
    }
}
